# Copyright (c) Ultraviolet
# SPDX-License-Identifier: Apache-2.0
#
# Local development override to expose the UI port directly.
# Used by `make up` targets so developers can access the UI on ${UI_PORT}.

services:
  ui:
    ports:
      - ${UI_PORT}:${UI_PORT}
